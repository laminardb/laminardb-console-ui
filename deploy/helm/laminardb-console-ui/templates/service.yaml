apiVersion: v1
kind: Service
metadata:
  name: {{ include "laminardb-console-ui.fullname" . }}
  labels:
    {{- include "laminardb-console-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "laminardb-console-ui.selectorLabels" . | nindent 4 }}
